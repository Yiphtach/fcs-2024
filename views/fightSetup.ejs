<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Choose Your Universe</title>
    <link rel="stylesheet" href="/styles/main.css"> <!-- Assume you have a main stylesheet for consistency -->
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
            padding: 0;
        }
        h1, h2 {
            color: #333;
        }
        .universe-selection {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin: 30px 0;
        }
        .universe-button {
            display: inline-block;
            padding: 15px 25px;
            background-color: #4CAF50;
            color: white;
            text-decoration: none;
            font-size: 18px;
            border-radius: 5px;
        }
        .universe-button:hover {
            background-color: #45a049;
        }
        .instructions {
            text-align: center;
            margin-bottom: 20px;
        }
    </style>
</head>
<body>
    <!-- Universe Selection Header -->
    <h1>Choose Your Universe</h1>
    <p class="instructions">Select the universe you want to fight in:</p>

    <!-- Universe Selection Buttons -->
    <div class="universe-selection">
        <% universes.forEach(universe => { %>
            <a href="/fights/selectCharacter?universe=<%= universe %>" class="universe-button"><%= universe %></a>
        <% }) %>
    </div>

    <!-- Footer and other includes -->
    <%- include('partials/footer') %> <!-- Footer partial -->

    <!-- Styles specific to dynamic health and fight details for the fight simulation part -->
    <style>
        .fight-log {
            list-style-type: none;
            padding: 0;
        }
        .fight-log li {
            background-color: #f2f2f2;
            padding: 10px;
            margin-bottom: 5px;
            border-radius: 5px;
        }
        .fight-winner {
            padding: 10px;
            background-color: #4CAF50;
            color: white;
            margin-top: 20px;
            font-weight: bold;
            border-radius: 5px;
        }
        .fight-details {
            margin-top: 20px;
        }
        .health-bars {
            display: flex;
            justify-content: space-between;
        }
        .health-bar {
            width: 45%;
            background-color: #ddd;
            border-radius: 5px;
            padding: 5px;
        }
        .health-bar-inner {
            height: 20px;
            background-color: #4CAF50;
            border-radius: 5px;
        }
        @media (max-width: 600px) {
            .health-bars {
                flex-direction: column;
            }
            .health-bar {
                width: 100%;
                margin-bottom: 10px;
            }
        }
    </style>
</body>
</html>
